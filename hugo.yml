module:
  mounts:
    - source: archetypes
      target: archetypes
    - source: assets
      target: assets
    - source: content
      target: content
    - source: i18n
      target: i18n
    - source: layouts
      target: layouts

# Images
params:
  persons:
    publications_persons:
      desktop: '100x100'
      mobile: '50x50'

# Default
permalinks:
  taxonomy:
    publications_persons: /publications/persons-concerned/
  term:
    publications_persons: /publications/persons-concerned/:slug/
taxonomies:
  publications_person: publications_persons
